import './skeleton.css'; // Import the CSS file for the skeleton styling

const Skeleton = () => {
  return (
    <div className="skeleton">
      <div className="skeleton-content"></div>
    </div>
  );
};

export default Skeleton;